import { mockSupabase } from '../../__mocks__/@supabase/supabase-js';

describe("serviceProvidersApi", () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test("dummy test to ensure suite passes", () => {
    expect(true).toBe(true);
  });
});
